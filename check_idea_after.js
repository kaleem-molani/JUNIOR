import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function check() { const idea = await prisma.symbol.findFirst({ where: { symbol: 'IDEA-EQ' } }); console.log('IDEA-EQ in DB after refresh:', idea); await prisma.$disconnect(); } check();
